import React from "react";

const Grocery = () => {
  return (
    <div>
      <h2>
        This is using code splitting using lazy loading to make different
        bundles to reduce the size of the app.
      </h2>
    </div>
  );
};

export default Grocery;
